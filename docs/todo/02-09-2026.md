# DeltaTok TODO — the queue

The work queue for [`ROADMAP.md`](../ROADMAP.md), the CVPR paper skeleton. `Paper` says which section or table each
item fills. A TODO becomes a `task` file in its thread when work starts, and its row here then points at that
file. `Status` is one of `not started`, `plan written`, `started`, `running`, `read landed`; `Note` carries the jobs, the read point and the task file.
Live job state, epochs reached and what to check next are on the jobs board [`STATUS.md`](STATUS.md), not here.
A row that reaches `done: <file>` or `dropped: <reason>` moves to [`DONE.md`](../DONE.md) keeping its `#`; nothing is
deleted. Previous backlog: `../research/plan/2026-08-18_cross_backlog.html`.

**Next big step (2026-09-02):** finalize the training and evaluation setting, then get baseline results. TODO 3 and 4;
they go before 2.

**This week (2026-09-02):** TODO 7 (pointdit choices), 8 (sum at 0.02 tc512), 10 (FVD on OccAny features).

| Status | # | Item | Thread | Paper | Note |
|---|---|---|---|---|---|
| read landed | 2 | tc512 + sigreg 0.02 / 0.04 / 0.08 | tc_width | ablation: SIGReg weight | `BSC:45296347 (0.02)` and `BSC:45297731 (0.04)` stopped at ep 81 on the wall, `BSC:45296349 (0.08)` cancelled at ep 32. Read at ep 67 and 81: `../research/results/2026-09-04_tc_width_tc512_sigreg_weight_axis_slides.html` — plateau 0.01–0.04 (6% spread), break at 0.08. Plan `../research/plan/2026-09-01_tc_width_sigreg_weight_tc512.md`, §4 still pending |
| not started | 3 | Finalize the training and evaluation setting | cross | main results: target setting | Open per `../ROADMAP.md` "Where the code is against it": OpenScene loader, training mixture, FVD or not, final eval sets |
| not started | 4 | Baseline results: DeltaTok, VGGT-World, Gen3R | cross | main results: main table | Depends on 3. Per `../ROADMAP.md` "Baselines": none run on our setting, Gen3R not in `third_party/` |
| started | 7 | Implement the pointdit choices in the flow trainer | pointdit | ablation: flow recipe | `BSC:45344713 cancelled 2026-09-04 at ep 58; read at matched ep 50: ../research/results/2026-09-04_pointdit_lowt_numsteps_ep50_slides.html — loses 19/20 cells`. Remaining arms gated on TODO 11. The shared `force_zero_t_ratio` patch landed in 911b026 (trainer + yaml key + startup print of the resolved `t` schedule), so the other two arms need no new code. **Submitted:** both knobs from scratch, `logitnormal(-0.8,0.8)` + 10% `t=0`, 100 ep, ~65 h in one 72 h BSC job. **Not submitted:** mitigation 1 alone, `../research/plan/2026-09-02_pointdit_lowt_recipe_finetune.md` (10% forced `t=0`, ~6.5 h fine-tune off iter 200000) — mostly redundant if the from-scratch arm moves; mitigation 2 alone, `../research/plan/2026-09-02_pointdit_xloss_additive.md` (unweighted x-MSE at weight 1.3) — the fallback if everything degrades. The zeros ODE init is `dropped: not important for us` — eval-time sampler knob, no plan file was written. Mechanism: `../research/analysis/2026-09-02_pointdit_vs_deltatok.md`. The fourth choice, the boundary-F1 realism probe, has no plan file yet. Inherited from [TODO 1](../DONE.md): re-run the `1,2,3,4,20` step sweep once a low-`t` arm exists, where N=1 is no longer starved |
| running | 6 | Attack the rank ceiling directly instead of via the weight | sigreg | contribution: SIGReg | `../research/plan/2026-09-02_sigreg_cov_penalty.md`. **Submitted 2026-09-04 as BSC:45416718**, 44 h on `ehpc880`, read at ep 67 against the twin. One arm: `training.cov_weight=3e-5` (a direct `‖E[zzᵀ]−I‖²_F/Cz` term on the rows SIGReg already pools) on top of the twin's recipe, tc512 · compose 1.0 · sigreg 0.02, twin BSC:45296347 at matched ep 67. Whitening and per-channel rescale are ruled out in §1 — both are gauge changes that remove the very anisotropy signal they would be applied to. Never a swap: SIGReg stays on. Base confirmed 2026-09-04 as **sigreg 0.02** (the twin) even though 0.01 is the axis best — see the plan's "A second bar": a win under ~6% is inside the weight plateau, so report against BSC:45106935 (0.01) too |
| read landed | 8 | Sum is actually better → run again with 0.02 tc512 (0.01 undecided); compare with 512 plain | sigreg | ablation: SIGReg on the composed sum | `BSC:45345063` cancelled 2026-09-04 mid-ep 67. Read at ep 65 against the twin BSC:45296347 in the same deck as TODO 2, slide 6: the sum's 0.005 win reverses to +8.0% eval `LossRecon` at 0.02 — a low-weight rescue, not a better objective. Plan `../research/plan/2026-09-02_sigreg_sum_at_weight_0.02.md`, §4 still pending |
| not started | 10 | Add FVD on OccAny features for eval: average patch tokens | flow | main results: main table | `occrae/util/training_summary.py:57` already consumes a `"FVD"` metric key and nothing emits it. Settles the FVD line in `../ROADMAP.md` "Target setting" |
| plan written | 11 | Can the eval see generation? best-of-K + K-spread + `train_fixed_t=0` regressor null | flow | main results: eval protocol | `../research/plan/2026-09-04_flow_bestofk_regressor_null.md`. Five eval jobs on existing ckpts (minutes each) + one 72 h regressor run read at `iter_100000`. Gates every further `t`-schedule / sampler arm, and the CFG and weight-cap arms |
| running | 12 | Decoder-only noise-augmented finetune of the tc128 compose tokenizer (RAE `noise_tau`), read through the unchanged flow control | flow | main results: tokenizer for the flow eval | `../research/plan/2026-09-04_flow_decoder_noise_finetune.md`. **Submitted 2026-09-04 as BSC:45421190**, 8 h on `ehpc1001`, 10 epochs warm-started weights-only from `compose1.0/ckpts/epoch_100.pth`, read at ep 10 (ep 5 as the backstop); still PENDING, not yet verified running. Decoder-only: `model.deltatok.decode_noise_tau=0.8` + `training.freeze_except_decoder=true`, so the encoder is frozen, `z` is bit-identical, every flow checkpoint stays valid, and the flow's `MSEToken` 0.6677 must reproduce exactly as the built-in check. The read is two eval jobs off `slurm/eval_deltatok_flow_numsteps_tc128compose_bsc.slurm` with `DELTATOK_CKPT` at the finetuned ckpt: flow N=1/20, and the `--noise_sigmas 0.0,0.32,0.55,0.82` ladder. Motivated by the probe `../research/results/2026-09-04_flow_decoder_noise_probe.md` (`BSC:45417908`): decoded loss is superlinear in latent error, knee below MSE 0.30, flow at 0.67 |
| running | 13 | End-to-end noisy-channel tokenizer at tc512: fold `z + σ·ε` into training with the encoder in the graph | tc_width | ablation: noise in the latent | `../research/plan/2026-09-07_tc_width_decnoise_e2e_tc512.md`. **Submitted 2026-09-07 as BSC:45529827.** Routed here by falsifier 3 of TODO 12's plan: *"the pair needs a from-scratch noisy tokenizer, which changes `z` and is a `tc_width`/`sigreg` question, not a flow one."* One arm — copy of `slurm/deltatok/train_deltatok_compose_sigreg_nozn_tc512_bsc.slurm` with `model.deltatok.decode_noise_tau=0.8` · `training.decode_noise_warmup=2000` · `training.eval_noise_sigmas=[0.32,0.55,0.82]`, `RUN_NAME=..._compose1.0_decnoise0.8`, 100 ep at 0.61 h/ep measured on the twin → ~61 h; shipped at 40 h a job per the BSC backfill convention, so it needs one chained resume. Unlike TODO 12 the encoder is **not** frozen: the add is differentiable, so the encoder optimises a code that must survive the channel, while SIGReg and the compose sum keep reading the clean `z`. Two new knobs (the τ ramp, and the tokenizer's own noise ladder as an `[Eval/…]` scalar — no tc512 flow checkpoint exists, so the ladder cannot be read the way TODO 12 read it) plus an eval-only job on the twin's `epoch_80.pth` for the "before" ladder. Control `BSC:45296347` at matched ep 40 / 72 / 80. Tripwire at ep 12. Motivation: the `cov_weight` dose-response (TODO 6) shows train `ZPartRank` monotone in dose to 141.0 with **none of it transferring** to recon, so a term that pushes `E[zzᵀ]→I` is answered; a noisy channel applies the pressure through the reconstruction loss instead |
