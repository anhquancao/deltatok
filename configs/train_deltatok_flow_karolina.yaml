defaults:
  - train_deltatok_flow
  - _self_

# The base config already uses Karolina dataset roots
# (/scratch/project/eu-25-92/...) — this overlay only exists so SLURM scripts
# can keep a dedicated config name for cluster runs.
